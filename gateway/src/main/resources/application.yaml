server:
  port: 8080
  

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: bloque_boleta
              uri: lb://jedis
              predicates:
                - Path=/api/v1/jedis/**, /jedis/api-docs/**

            - id: bloque_producto
              uri: lb://sables
              predicates:
                - Path=/api/v1/cristales/**, /cristales/api-docs/**

            - id: bloque_usuario
              uri: lb://sables
              predicates:
                - Path=/api/v1/sables/**, /sables/api-docs/**

springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
    urls:
      - name: Microservicio Jedis
        url: /jedis/api-docs
      - name: Microservicio Sables
        url: /sables/api-docs
  api-docs:
    enabled: true